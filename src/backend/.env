APP_ENV=dev
CORS_ORIGINS=http://localhost:5173

# Refresh datasets automatically every N minutes
DATA_REFRESH_MINUTES=60

# HuggingFace datasets to stream (small demo-safe sets)
HF_DATASETS=ag_news,emotion
